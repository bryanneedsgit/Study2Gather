import { Text } from "react-native";
import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function LockInScreen() {
  return (
    <PlaceholderScreen title="Lock-In" subtitle="Group study timer and guardrails.">
      <Text>Upcoming: lock-in timer, foreground checks, co-presence gating.</Text>
    </PlaceholderScreen>
  );
}
