import { Text } from "react-native";
import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function RewardsScreen() {
  return (
    <PlaceholderScreen title="Rewards" subtitle="Track earned points and ledger entries.">
      <Text>Upcoming: per-session points history and totals.</Text>
    </PlaceholderScreen>
  );
}
