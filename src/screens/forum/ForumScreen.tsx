import { Text } from "react-native";
import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function ForumScreen() {
  return (
    <PlaceholderScreen title="Forum" subtitle="Strictly text-only academic problem-solving.">
      <Text>Upcoming: threads and comments with text validation only.</Text>
    </PlaceholderScreen>
  );
}
